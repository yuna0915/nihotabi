<h1 class="text-center mt-5 mb-4">フォローしている人の投稿一覧</h1>

<% if @posts.any? %>
  <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
    <%= render partial: "public/posts/post_card", collection: @posts, as: :post, locals: { from: 'follow_feed', genre_id: nil } %>
  </div>

  <div class="d-flex justify-content-center my-4">
    <%= paginate @posts %>
  </div>

<% else %>
  <p class="text-center">フォローしている人の投稿はまだありません。</p>
<% end %>

<div class="text-center mt-5 mb-5">
  <%= link_to 'マイページへ戻る', my_page_user_path(current_user), class: 'custom-outline-btn' %>
</div>
