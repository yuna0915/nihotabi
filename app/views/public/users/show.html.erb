<h1 class="text-center mt-5 mb-4">ユーザー情報</h1>

<div class="d-flex justify-content-center">
  <table class="table table-borderless w-auto text-start">
    <tbody>
      <tr>
        <td>プロフィール画像</td>
        <td>
          <%= image_tag @user.get_profile_image(120, 120), class: "rounded-circle" %>
        </td>
      </tr>
      <% if @user.introduction.present? %>
        <tr>
          <td>自己紹介文</td>
          <td><%= simple_format(@user.introduction) %></td>
        </tr>
      <% end %>
      <tr>
        <td>氏名</td>
        <td>姓：<%= @user.last_name %></td>
      </tr>
      <tr>
        <td></td>
        <td>名：<%= @user.first_name %></td>
      </tr>
      <tr>
        <td>フリガナ</td>
        <td>セイ：<%= @user.last_name_kana %></td>
      </tr>
      <tr>
        <td></td>
        <td>メイ：<%= @user.first_name_kana %></td>
      </tr>
      <tr>
        <td>ニックネーム</td>
        <td><%= @user.nickname %></td>
      </tr>
      <tr>
        <td>メールアドレス</td>
        <td><%= @user.email %></td>
      </tr>
      <tr>
        <td>電話番号（ハイフンなし）</td>
        <td><%= @user.phone_number %></td>
      </tr>
      <tr>
        <td>都道府県</td>
        <td><%= @user.prefecture.is_a?(Prefecture) ? @user.prefecture.name : @user.prefecture %></td>
      </tr>
    </tbody>
  </table>
</div>

<div class="text-center mt-4 mb-5">
  <% if @user == current_user %>
    <% if current_user.email == "guest@example.com" %>
      <p class="mt-3">
        プロフィールを編集するには、
        <%= link_to "新規登録", destroy_user_session_path + "?redirect_to=sign_up", method: :delete, data: { confirm: "ゲストユーザーをログアウトして新規会員登録画面へ移動します。よろしいですか？" } %>
        または
        <%= link_to "ログイン", destroy_user_session_path + "?redirect_to=login", method: :delete, data: { confirm: "ゲストユーザーをログアウトしてログイン画面へ移動します。よろしいですか？" } %>
        が必要です。
      </p>
    <% else %>
      <div class="mb-3">
        <%= link_to "プロフィールを編集", edit_user_path(@user), class: "custom-green-btn me-2 mb-2" %>
        <%= link_to "退会する", withdraw_user_path, method: :patch, data: { confirm: "本当に退会しますか？" }, class: "custom-red-btn mb-2" %>
      </div>
    <% end %>
  <% end %>

  <% if user_signed_in? && current_user == @user %>
    <div class="mt-5 mb-5">
      <%= link_to "マイページへ戻る", my_page_user_path(@user), class: "custom-outline-btn" %>
    </div>
  <% end %>
</div>
